# codex-0.153.4-patch.2

## Source

- Upstream: `openai/codex@rust-v0.153.4`
- Upstream SHA: `3d2ee51ca2d5db578f328aa75e20aa22c0197c9a`
- Patch release: `codex-0.153.4-patch.2`

## Gate

- Enabled patches applied with `git apply --check`.
- `nix flake check` passed on `main` (`24c6597`) via this release's own PR CI before tagging.
- GitHub Actions builds release artifacts from the tag.

## Enabled Patches

- `tui-notification-sound`
- `plan-mode-default`
- `timed-queue`
- `tui-status-line-command`
- `editable-enter-queue`

## Why patch.2

`tui-status-line-command` had a passive-footer height bug: a 2-line
`[tui.status_line_command]` output was rendered into a 1-row reserved area, so
the second row was clipped. Fixed in two commits on `main`
(`bd95e19`, `24c6597`) -- the second commit fixes a call site the first missed
(`history_search.rs`), caught by this release's own CI build rather than
locally.
