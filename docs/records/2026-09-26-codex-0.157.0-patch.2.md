# codex-0.157.0-patch.2

## Release scope

- Upstream: `openai/codex@rust-v0.157.0`, commit `00c972ed5d6ff6499317fd41b7f23605b8e6850d`.
- Baseline: published [patch.1](2026-09-25-codex-0.157.0-patch.1.md), followed by the release automation fixes on main.
- Feature: [PR #8](https://github.com/salty-flower/codex-patch-overlay/pull/8), rebased onto that baseline.
- Patch stack: the twelve baseline patches plus `tui-reasoning-display`.

## Reasoning display

| Setting | Behavior |
| --- | --- |
| `status-only` | Default upstream status row and expanded transcript behavior |
| `persistent` | Live summary preview and completed summaries in ordinary scrollback, including restored history |
| `hidden` | Suppressed summaries and generic working status |

`show_raw_agent_reasoning` independently controls raw reasoning in all three modes.
`model_reasoning_summary` must request summaries from a supporting model; the display setting itself does not change model requests.

Live previews use their own history cell and preserve the active tool's identity.
If a summary completes while a tool still runs, the tool remains at the live tail and continues receiving output and completion updates.
Completed tool groups retain ordinary history insertion order.
Authoritative completion replaces partial streamed content; an absent completion body retains the received stream.
Summary item IDs prevent stale events and duplicate completion from adding another copy.
Safety buffering hides provisional previews and restores them when waiting ends.
History pagination, transcript export, and local or remote resume use the configured display mode.

## Verification

All thirteen patches apply cumulatively through both `git apply --check` and Nix's `patch -p2` convention.
The reconstructed source matches the implementation, including both carried app-server schema exports.
The test checkout differs only in the upstream fixture-required workspace version `0.0.0` and generated test directories.
The config schema was regenerated with upstream's schema writer.
All 21 focused configuration, streaming, completion, tool-lifecycle, replay, and projection regressions passed on macOS.
The actual main viewport and expanded transcript were rendered and inspected at 24 and 80 columns.

The full macOS suite exposed upstream timer/spinner snapshot races in `lost_mutation_reply_preserves_work_without_resubmitting` and `guardian_approved_request_permissions_clears_status_without_history`.
The existing `tui-snapshot-stability` workaround now freezes those fixtures' status timers and normalizes their spinners, preserving their golden snapshots and behavior assertions.
The full nine-crate run completed with 9,829 passes and these two snapshot failures, with five upstream skips.
Both failed cases passed after the test-only fixes; the complete output was reviewed.
The public core API sample passes `cargo check`, retaining its single Codex dependency by initializing the new display setting through `Default`.
All 21 carried account-switching, login, and WebSocket integration scenarios pass on macOS.

The shared CI Nix install action selects free builder UIDs within the runner's system-user range, avoiding the upstream installer's UID-range warnings while preserving daemon isolation.
The final Linux installation created all eight builders at UIDs 980–987 within the runner's active system range of 101–999.

The [final PR checks](https://github.com/salty-flower/codex-patch-overlay/actions/runs/36168917350) passed on `d5d932326aa30d4924ea5868a0fcddf98217080b`.
Linux passed all 9,812 unit tests, with six upstream skips, the linked V8 sandbox assertion, the public core API sample check, and all 21 carried integration scenarios.
The complete test and Nix job logs were reviewed.
`nix flake check --all-systems` passed on Linux and locally on Apple Silicon, evaluating all three declared platforms and building the native checks.
The final macOS Nix package passed native smoke checks for all three Rust executables.
The release-only unused-helper warnings were removed by restricting the legacy transcript wrappers to tests.

PR #8 was squash merged as `d00e58b2b83ad06951f6ca9f020c6d1b5f01c9ee`.
Its tree exactly matches the verified PR head; `codex-0.157.0-patch.2` tags that merge commit.

The first release attempt stopped during Linux OpenSSL setup because the CI-only metadata patch had been generated from a fixture with different blank lines from the checksum-verified release archive.
The patch was regenerated against the actual OpenSSL 3.6.4 archive.
Release retries read these CI-only installer patches from the exact workflow commit, keeping the application source tag unchanged.
Whitespace hooks also exclude these patch containers so that formatting cannot rewrite their context lines.

The previously approved `proc-macro-error2 2.0.1` future-compatibility warning remains the sole compiler-warning exception.

## Published packages

Published release: [codex-0.157.0-patch.2](https://github.com/salty-flower/codex-patch-overlay/releases/tag/codex-0.157.0-patch.2).
The [release workflow](https://github.com/salty-flower/codex-patch-overlay/actions/runs/36180257123) completed successfully for both build jobs and publication.
It used workflow commit `e7fc80d18fff5ba50368772a8e2c711407206503` to build the unchanged tagged application source at `d00e58b2b83ad06951f6ca9f020c6d1b5f01c9ee`.
The superseded first attempt was canceled after its Linux preparation failure; no artifacts from that attempt were published.

Both final build logs were reviewed in full.
Each platform passed the linked V8 sandbox assertion, upstream package layout validation, and native smoke checks for all three Rust executables.
The classic macOS linker and corrected OpenSSL metadata helper emitted none of the earlier diagnostics.
The only compiler warning was the previously waived dependency warning.

Both published archives were downloaded and passed their SHA-256 sidecars, binary architecture checks, package metadata checks, and exact tagged-manifest comparison.
The bundled ripgrep and zsh executables matched the previously checksum-verified upstream helpers.
All three macOS executables passed native smoke checks again after download, with empty stderr.
The release contains exactly these two archives and their two checksum files.

| Archive target | Bytes | SHA-256 |
| --- | --- | --- |
| `aarch64-apple-darwin` | 162557132 | `8e9fc1829614409a0dc0eb2d99738aa148d752dbbd7fb07fccceb7b012369dbb` |
| `x86_64-unknown-linux-musl` | 467523797 | `a05a4fac74f6cd805ac44c6c587665310ad4d75fec669d6c9d4bd25295f9d165` |

The publish job moved `latest-release` automatically.
Remote ref verification confirmed it resolves to the tag's commit `d00e58b2b83ad06951f6ca9f020c6d1b5f01c9ee`, independently of later release-automation and documentation commits on main.
