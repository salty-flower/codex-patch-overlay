# codex-0.144.4-patch.4

## Source

- Upstream: `openai/codex@rust-v0.144.4`
- Upstream SHA: `8c68d4c87dc54d38861f5114e920c3de2efa5876`
- Patch release: `codex-0.144.4-patch.4`
- Release type: GitHub release
- Supersedes: `codex-0.144.4-patch.3`

## Fixes

- Status-line snapshot `approval_mode` mirrors the built-in approval-mode item, so the
  on-request reviewer toggle serializes distinct values (`approve-for-me` /
  `ask-for-approval`) and re-renders the command-backed status line. Previously both
  states serialized as `on-request` and the toggle never triggered a refresh.
- Wire change: consumers matching `approval_mode == "on-request"` must switch to the
  two reviewer-specific values; other policies are unchanged.

## Gate

- Enabled patches applied with `git apply --check`.
- `nix flake check` skipped locally; the tag build in CI gates the release.
- GitHub Actions builds release artifacts from the tag.
- `latest-release` moves only after successful artifact publication.

## Enabled Patches

- `stream-reasoning-live`
- `tui-notification-sound`
- `webp-image-input`
- `token-usage-breakdown`
- `plan-mode-default`
- `timed-queue`
- `auto-thread-name`
- `tui-status-line-command`
