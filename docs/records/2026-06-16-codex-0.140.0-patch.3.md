# codex-0.140.0-patch.3

## Source

- Upstream: `openai/codex@rust-v0.140.0`
- Upstream SHA: `6506579001c322927a3e4bd440563267a7ac6c1f`
- Patch release: `codex-0.140.0-patch.3`

## Gate

- Enabled patches applied with `git apply --check`.
- Auto-release dry run passed for `rust-v0.140.0`.
- GitHub Actions builds and verifies release artifacts from the tag.

## Notes

- Keeps the `patch.2` scheduled-input fix.
- Fixes the `patch.2` macOS CI failure where Cargo's default git fetch path
  timed out fetching `libwebrtc`'s `libyuv` submodule from
  `chromium.googlesource.com`.
- Release CI now uses Cargo's git CLI fetch mode and a higher Cargo retry
  count for git dependencies.

## Enabled Patches

- `stream-reasoning-live`
- `tui-notification-sound`
- `webp-image-input`
- `disable-autocompact`
- `token-usage-breakdown`
- `plan-mode-default`
- `timed-queue`
