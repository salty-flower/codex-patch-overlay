# codex-0.152.1-patch.2

## Source

- Upstream: `openai/codex@rust-v0.152.1`
- Upstream SHA: `5adb68a49933ae446bf11935662c83dba55a0804`
- Patch release: `codex-0.152.1-patch.2`

## Fix

- Restore command-backed status-line precedence when a configured built-in status line is active.
- Keep transient flash and hint overrides ahead of the passive command-backed status line.

## Gate

- Enabled patches applied with `git apply --check` against the staged upstream source.
- The targeted Ratatui render regression passed: command output replaces the configured built-in line.
- GitHub Actions runs the full check and release builds from the pushed commit and tag.

## Enabled Patches

- `tui-notification-sound`
- `plan-mode-default`
- `timed-queue`
- `tui-status-line-command`
