diff --git a/codex-rs/tui/src/app/event_dispatch.rs b/codex-rs/tui/src/app/event_dispatch.rs
index 6c6a31ee6..9587f871a 100644
--- a/codex-rs/tui/src/app/event_dispatch.rs
+++ b/codex-rs/tui/src/app/event_dispatch.rs
@@ -1816,6 +1816,18 @@ impl App {
                 self.chat_widget
                     .submit_user_message_with_mode(text, collaboration_mode);
             }
+            AppEvent::ScheduledQueueDue { thread_id } => {
+                if self.chat_widget.thread_id() == Some(thread_id) {
+                    self.chat_widget.maybe_send_next_queued_input();
+                    tui.frame_requester().schedule_frame();
+                }
+            }
+            AppEvent::ScheduledQueueTick { thread_id } => {
+                if self.chat_widget.thread_id() == Some(thread_id) {
+                    self.chat_widget.refresh_pending_input_preview();
+                    tui.frame_requester().schedule_frame();
+                }
+            }
             AppEvent::ManageSkillsClosed => {
                 self.chat_widget.handle_manage_skills_closed();
             }
diff --git a/codex-rs/tui/src/app_event.rs b/codex-rs/tui/src/app_event.rs
index 39e0b9503..ba9763380 100644
--- a/codex-rs/tui/src/app_event.rs
+++ b/codex-rs/tui/src/app_event.rs
@@ -888,6 +888,16 @@ pub(crate) enum AppEvent {
         collaboration_mode: CollaborationModeMask,
     },
 
+    /// A scheduled follow-up prompt reached its due time.
+    ScheduledQueueDue {
+        thread_id: ThreadId,
+    },
+
+    /// Periodic tick to refresh the countdown display for scheduled prompts.
+    ScheduledQueueTick {
+        thread_id: ThreadId,
+    },
+
     /// Open the approval popup.
     FullScreenApprovalRequest(ApprovalRequest),
 
diff --git a/codex-rs/tui/src/chatwidget.rs b/codex-rs/tui/src/chatwidget.rs
index fb89fda2e..6109b06d4 100644
--- a/codex-rs/tui/src/chatwidget.rs
+++ b/codex-rs/tui/src/chatwidget.rs
@@ -359,6 +359,7 @@ use self::session_header::SessionHeader;
 mod hook_lifecycle;
 mod hooks;
 mod interaction;
+mod scheduled_input;
 mod skills;
 mod slash_dispatch;
 use self::skills::collect_tool_mentions;
diff --git a/codex-rs/tui/src/chatwidget/input_flow.rs b/codex-rs/tui/src/chatwidget/input_flow.rs
index b24a4fde2..119199889 100644
--- a/codex-rs/tui/src/chatwidget/input_flow.rs
+++ b/codex-rs/tui/src/chatwidget/input_flow.rs
@@ -91,6 +91,9 @@ impl ChatWidget {
                     user_message,
                     action,
                     pending_pastes,
+                    not_before: None,
+                    schedule_label: None,
+                    schedule_target_time: None,
                 });
             self.input_queue
                 .queued_user_message_history_records
@@ -111,7 +114,9 @@ impl ChatWidget {
         }
         let mut submitted_follow_up = false;
         while !self.is_user_turn_pending_or_running() {
-            let Some((queued_message, history_record)) = self.pop_next_queued_user_message() else {
+            let Some((queued_message, history_record)) =
+                self.pop_next_ready_queued_user_message(std::time::Instant::now())
+            else {
                 break;
             };
             match queued_message.action {
@@ -143,6 +148,49 @@ impl ChatWidget {
         submitted_follow_up
     }
 
+    pub(super) fn schedule_user_message(
+        &mut self,
+        user_message: UserMessage,
+        delay: std::time::Duration,
+        label: String,
+    ) {
+        let Some(thread_id) = self.thread_id else {
+            self.add_error_message(
+                "Session is still starting; try /schedule again in a moment.".to_string(),
+            );
+            return;
+        };
+        let not_before = std::time::Instant::now() + delay;
+        let target_time = chrono::Local::now() + delay;
+        self.input_queue.queued_user_messages.push_back(
+            QueuedUserMessage::from(user_message)
+                .scheduled_for(not_before)
+                .with_schedule_label(label.clone())
+                .with_schedule_target_time(target_time),
+        );
+        self.input_queue
+            .queued_user_message_history_records
+            .push_back(UserMessageHistoryRecord::UserMessageText);
+        self.refresh_pending_input_preview();
+        self.add_info_message(format!("Scheduled follow-up input {label}."), None);
+        let tx = self.app_event_tx.clone();
+        tokio::spawn(async move {
+            tokio::time::sleep(delay).await;
+            tx.send(AppEvent::ScheduledQueueDue { thread_id });
+        });
+        let tx = self.app_event_tx.clone();
+        tokio::spawn(async move {
+            let deadline = tokio::time::Instant::now() + delay;
+            loop {
+                tokio::time::sleep(std::time::Duration::from_secs(10)).await;
+                if tokio::time::Instant::now() >= deadline {
+                    break;
+                }
+                tx.send(AppEvent::ScheduledQueueTick { thread_id });
+            }
+        });
+    }
+
     pub(super) fn is_user_turn_pending_or_running(&self) -> bool {
         self.input_queue.user_turn_pending_start || self.bottom_pane.is_task_running()
     }
@@ -157,7 +205,7 @@ impl ChatWidget {
     }
 
     /// Rebuild and update the bottom-pane pending-input preview.
-    pub(super) fn refresh_pending_input_preview(&mut self) {
+    pub(crate) fn refresh_pending_input_preview(&mut self) {
         let preview = self.input_queue.preview();
         self.bottom_pane.set_pending_input_preview(
             preview.queued_messages,
diff --git a/codex-rs/tui/src/chatwidget/input_queue.rs b/codex-rs/tui/src/chatwidget/input_queue.rs
index 67a306af2..cca2a4f3c 100644
--- a/codex-rs/tui/src/chatwidget/input_queue.rs
+++ b/codex-rs/tui/src/chatwidget/input_queue.rs
@@ -4,6 +4,10 @@
 //! apply UI/protocol effects around a focused reducer-style state bag.
 
 use std::collections::VecDeque;
+use std::time::Instant;
+
+use chrono::DateTime;
+use chrono::Local;
 
 use super::PendingSteer;
 use super::QueuedUserMessage;
@@ -59,16 +63,49 @@ impl InputQueueState {
         self.submit_pending_steers_after_interrupt = false;
     }
 
+    pub(super) fn pop_next_ready_queued_user_message(
+        &mut self,
+        now: Instant,
+    ) -> Option<(QueuedUserMessage, UserMessageHistoryRecord)> {
+        if !self
+            .queued_user_messages
+            .front()
+            .is_some_and(|message| message.is_ready_at(now))
+        {
+            return None;
+        }
+        let message = self.queued_user_messages.pop_front()?;
+        let history = self
+            .queued_user_message_history_records
+            .pop_front()
+            .unwrap_or(UserMessageHistoryRecord::UserMessageText);
+        Some((message, history))
+    }
+
     pub(super) fn preview(&self) -> PendingInputPreview {
+        let now = Local::now();
         let queued_messages = self
             .queued_user_messages
             .iter()
             .enumerate()
             .map(|(idx, message)| {
-                user_message_preview_text(
+                let text = user_message_preview_text(
                     message,
                     self.queued_user_message_history_records.get(idx),
-                )
+                );
+                match &message.schedule_target_time {
+                    Some(target) => {
+                        let countdown = format_countdown(*target, now);
+                        format!(
+                            "{text} (scheduled {target} \u{2014} {countdown})",
+                            target = target.format("%H:%M:%S"),
+                        )
+                    }
+                    None => match &message.schedule_label {
+                        Some(label) => format!("{text} (scheduled {label})"),
+                        None => text,
+                    },
+                }
             })
             .collect();
         let pending_steers = self
@@ -95,6 +132,24 @@ impl InputQueueState {
     }
 }
 
+fn format_countdown(target: DateTime<Local>, now: DateTime<Local>) -> String {
+    let remaining = target.signed_duration_since(now);
+    let total_secs = remaining.num_seconds();
+    if total_secs <= 0 {
+        return "due".to_string();
+    }
+    let hours = total_secs / 3600;
+    let minutes = (total_secs % 3600) / 60;
+    let seconds = total_secs % 60;
+    if hours > 0 {
+        format!("{hours}h {minutes:02}m {seconds:02}s left")
+    } else if minutes > 0 {
+        format!("{minutes}m {seconds:02}s left")
+    } else {
+        format!("{seconds}s left")
+    }
+}
+
 #[cfg(test)]
 mod tests {
     use pretty_assertions::assert_eq;
@@ -129,6 +184,92 @@ mod tests {
         );
     }
 
+    #[test]
+    fn next_ready_queued_message_waits_for_scheduled_time() {
+        let mut state = InputQueueState::default();
+        let now = std::time::Instant::now();
+        state.queued_user_messages.push_back(
+            QueuedUserMessage::from(UserMessage::from("later"))
+                .scheduled_for(now + std::time::Duration::from_secs(60)),
+        );
+        state
+            .queued_user_message_history_records
+            .push_back(UserMessageHistoryRecord::UserMessageText);
+
+        assert!(state.pop_next_ready_queued_user_message(now).is_none());
+
+        let (message, history) = state
+            .pop_next_ready_queued_user_message(now + std::time::Duration::from_secs(60))
+            .expect("scheduled message should be ready at its due time");
+        assert_eq!(message.text, "later");
+        assert_eq!(history, UserMessageHistoryRecord::UserMessageText);
+        assert!(state.queued_user_messages.is_empty());
+        assert!(state.queued_user_message_history_records.is_empty());
+    }
+
+    #[test]
+    fn format_countdown_shows_hours_minutes_seconds() {
+        let target = Local::now() + chrono::Duration::seconds(3661);
+        let now = Local::now();
+        let text = format_countdown(target, now);
+        assert!(text.starts_with("1h 01m"), "expected 1h 01m, got: {text}");
+        assert!(
+            text.ends_with("s left"),
+            "expected trailing 's left', got: {text}"
+        );
+    }
+
+    #[test]
+    fn format_countdown_shows_minutes_seconds() {
+        let now = Local::now();
+        let target = now + chrono::Duration::seconds(125);
+        assert_eq!(format_countdown(target, now), "2m 05s left");
+    }
+
+    #[test]
+    fn format_countdown_shows_seconds_only() {
+        let now = Local::now();
+        let target = now + chrono::Duration::seconds(45);
+        assert_eq!(format_countdown(target, now), "45s left");
+    }
+
+    #[test]
+    fn format_countdown_past_target_shows_due() {
+        let now = Local::now();
+        let target = now - chrono::Duration::seconds(5);
+        assert_eq!(format_countdown(target, now), "due");
+    }
+
+    #[test]
+    fn preview_scheduled_message_shows_target_time_and_countdown() {
+        let mut state = InputQueueState::default();
+        let target = Local::now() + chrono::Duration::seconds(600);
+        state.queued_user_messages.push_back(
+            QueuedUserMessage::from(UserMessage::from("later"))
+                .scheduled_for(std::time::Instant::now() + std::time::Duration::from_secs(600))
+                .with_schedule_label("in 10m".to_string())
+                .with_schedule_target_time(target),
+        );
+        state
+            .queued_user_message_history_records
+            .push_back(UserMessageHistoryRecord::UserMessageText);
+
+        let preview = state.preview();
+        let msg = &preview.queued_messages[0];
+        assert!(
+            msg.contains(&target.format("%H:%M:%S").to_string()),
+            "expected target time in preview, got: {msg}"
+        );
+        assert!(
+            msg.contains("left"),
+            "expected countdown in preview, got: {msg}"
+        );
+        assert!(
+            msg.contains("\u{2014}"),
+            "expected em-dash separator, got: {msg}"
+        );
+    }
+
     #[test]
     fn clear_resets_all_input_queues() {
         let mut state = InputQueueState::default();
diff --git a/codex-rs/tui/src/chatwidget/input_restore.rs b/codex-rs/tui/src/chatwidget/input_restore.rs
index bcc4e956a..14813d841 100644
--- a/codex-rs/tui/src/chatwidget/input_restore.rs
+++ b/codex-rs/tui/src/chatwidget/input_restore.rs
@@ -55,21 +55,12 @@ impl ChatWidget {
         }
     }
 
-    pub(super) fn pop_next_queued_user_message(
+    pub(super) fn pop_next_ready_queued_user_message(
         &mut self,
+        now: std::time::Instant,
     ) -> Option<(QueuedUserMessage, UserMessageHistoryRecord)> {
         if self.input_queue.rejected_steers_queue.is_empty() {
-            self.input_queue
-                .queued_user_messages
-                .pop_front()
-                .map(|user_message| {
-                    let history_record = self
-                        .input_queue
-                        .queued_user_message_history_records
-                        .pop_front()
-                        .unwrap_or(UserMessageHistoryRecord::UserMessageText);
-                    (user_message, history_record)
-                })
+            self.input_queue.pop_next_ready_queued_user_message(now)
         } else {
             let rejected_messages = self
                 .input_queue
diff --git a/codex-rs/tui/src/chatwidget/slash_dispatch.rs b/codex-rs/tui/src/chatwidget/slash_dispatch.rs
index 32ab70ed3..adca19806 100644
--- a/codex-rs/tui/src/chatwidget/slash_dispatch.rs
+++ b/codex-rs/tui/src/chatwidget/slash_dispatch.rs
@@ -14,6 +14,7 @@ use crate::bottom_pane::slash_commands::SlashCommandItem;
 use crate::bottom_pane::slash_commands::find_slash_command;
 use crate::goal_display::GOAL_USAGE;
 use crate::goal_files::GoalDraft;
+use chrono::Local;
 
 #[derive(Clone, Copy, Debug, PartialEq, Eq)]
 enum SlashCommandDispatchSource {
@@ -36,6 +37,8 @@ const SIDE_SLASH_COMMAND_UNAVAILABLE_HINT: &str =
     "Press Ctrl+C to return to the main thread first.";
 const GOAL_USAGE_HINT: &str = "Example: /goal improve benchmark coverage";
 const RAW_USAGE: &str = "Usage: /raw [on|off]";
+const SCHEDULE_USAGE: &str = "Usage: /schedule in <duration> <prompt> OR /schedule at <HH:MM> <prompt>\n\
+     Duration: 10m, 1h30m, 90s, 1h2m3s (bare number = minutes)";
 const USAGE_CHATGPT_LOGIN_REQUIRED: &str = "Sign in with ChatGPT to use /usage.";
 
 impl ChatWidget {
@@ -256,6 +259,9 @@ impl ChatWidget {
             SlashCommand::Review => {
                 self.open_review_popup();
             }
+            SlashCommand::Schedule => {
+                self.add_info_message(SCHEDULE_USAGE.to_string(), None);
+            }
             SlashCommand::Rename => {
                 self.session_telemetry
                     .counter("codex.thread.rename", /*inc*/ 1, &[]);
@@ -711,6 +717,27 @@ impl ChatWidget {
                 };
                 self.app_event_tx.set_thread_name(name);
             }
+            SlashCommand::Schedule if !trimmed.is_empty() => {
+                let parsed =
+                    match scheduled_input::parse_schedule_args(trimmed, Local::now().time()) {
+                        Ok(parsed) => parsed,
+                        Err(message) => {
+                            self.add_error_message(message);
+                            return;
+                        }
+                    };
+                self.schedule_user_message(
+                    UserMessage {
+                        text: parsed.prompt,
+                        local_images: Vec::new(),
+                        remote_image_urls: Vec::new(),
+                        text_elements: Vec::new(),
+                        mention_bindings: Vec::new(),
+                    },
+                    parsed.delay,
+                    parsed.label,
+                );
+            }
             SlashCommand::Plan if !trimmed.is_empty() => {
                 if !self.apply_plan_slash_command() {
                     return;
@@ -1048,6 +1075,7 @@ impl ChatWidget {
             | SlashCommand::Diff
             | SlashCommand::App
             | SlashCommand::Rename
+            | SlashCommand::Schedule
             | SlashCommand::TestApproval => QueueDrain::Continue,
             SlashCommand::Feedback
             | SlashCommand::New
diff --git a/codex-rs/tui/src/chatwidget/user_messages.rs b/codex-rs/tui/src/chatwidget/user_messages.rs
index 6cfb739da..c79371498 100644
--- a/codex-rs/tui/src/chatwidget/user_messages.rs
+++ b/codex-rs/tui/src/chatwidget/user_messages.rs
@@ -10,6 +10,10 @@ use std::collections::HashSet;
 use std::collections::VecDeque;
 use std::ops::Deref;
 use std::path::PathBuf;
+use std::time::Instant;
+
+use chrono::DateTime;
+use chrono::Local;
 
 use crate::bottom_pane::LocalImageAttachment;
 use crate::bottom_pane::MentionBinding;
@@ -61,6 +65,9 @@ pub(super) struct QueuedUserMessage {
     pub(super) user_message: UserMessage,
     pub(super) action: QueuedInputAction,
     pub(super) pending_pastes: Vec<(String, String)>,
+    pub(super) not_before: Option<Instant>,
+    pub(super) schedule_label: Option<String>,
+    pub(super) schedule_target_time: Option<DateTime<Local>>,
 }
 
 impl QueuedUserMessage {
@@ -69,12 +76,34 @@ impl QueuedUserMessage {
             user_message,
             action,
             pending_pastes: Vec::new(),
+            not_before: None,
+            schedule_label: None,
+            schedule_target_time: None,
         }
     }
 
+    pub(super) fn scheduled_for(mut self, not_before: Instant) -> Self {
+        self.not_before = Some(not_before);
+        self
+    }
+
+    pub(super) fn with_schedule_label(mut self, label: String) -> Self {
+        self.schedule_label = Some(label);
+        self
+    }
+
+    pub(super) fn with_schedule_target_time(mut self, target: DateTime<Local>) -> Self {
+        self.schedule_target_time = Some(target);
+        self
+    }
+
     pub(super) fn into_user_message(self) -> UserMessage {
         self.user_message
     }
+
+    pub(super) fn is_ready_at(&self, now: Instant) -> bool {
+        self.not_before.is_none_or(|not_before| now >= not_before)
+    }
 }
 
 impl From<UserMessage> for QueuedUserMessage {
diff --git a/codex-rs/tui/src/slash_command.rs b/codex-rs/tui/src/slash_command.rs
index 05dcefe93..c494bcb9e 100644
--- a/codex-rs/tui/src/slash_command.rs
+++ b/codex-rs/tui/src/slash_command.rs
@@ -29,6 +29,7 @@ pub enum SlashCommand {
     Import,
     Hooks,
     Review,
+    Schedule,
     Rename,
     New,
     Archive,
@@ -87,6 +88,7 @@ impl SlashCommand {
             SlashCommand::Init => "create an AGENTS.md file with instructions for Codex",
             SlashCommand::Compact => "summarize conversation to prevent hitting the context limit",
             SlashCommand::Review => "review my current changes and find issues",
+            SlashCommand::Schedule => "e.g. /schedule in 5m <prompt> or at 14:30 <prompt>",
             SlashCommand::Rename => "rename the current thread",
             SlashCommand::Resume => "resume a saved chat",
             SlashCommand::Archive => "archive this session and exit",
@@ -154,6 +156,7 @@ impl SlashCommand {
         matches!(
             self,
             SlashCommand::Review
+                | SlashCommand::Schedule
                 | SlashCommand::Rename
                 | SlashCommand::Plan
                 | SlashCommand::Goal
@@ -214,6 +217,7 @@ impl SlashCommand {
             | SlashCommand::Copy
             | SlashCommand::Raw
             | SlashCommand::Rename
+            | SlashCommand::Schedule
             | SlashCommand::Mention
             | SlashCommand::Skills
             | SlashCommand::Hooks
