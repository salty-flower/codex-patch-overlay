diff --git a/codex-rs/config/src/types.rs b/codex-rs/config/src/types.rs
index c78ed6986..479cb151c 100644
--- a/codex-rs/config/src/types.rs
+++ b/codex-rs/config/src/types.rs
@@ -673,6 +673,40 @@ pub struct TuiNotificationSettings {
     /// regardless of focus. Defaults to `unfocused`.
     #[serde(default, rename = "notification_condition")]
     pub condition: NotificationCondition,
+
+    /// Optional sound command to run after a TUI desktop notification.
+    ///
+    /// When unset, Codex uses a platform default sound command. Set
+    /// `enabled = false` to disable the extra sound, or set `command` to run a
+    /// custom argv array.
+    ///
+    /// ```toml
+    /// [tui]
+    /// notification_sound = { command = ["afplay", "/path/to/done.aiff"] }
+    /// ```
+    #[serde(default, rename = "notification_sound")]
+    pub sound: TuiNotificationSoundConfig,
+}
+
+#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, JsonSchema)]
+#[schemars(deny_unknown_fields)]
+pub struct TuiNotificationSoundConfig {
+    /// Enable the extra notification sound. Defaults to `true`.
+    #[serde(default = "default_true")]
+    pub enabled: bool,
+
+    /// Custom sound command argv. The first item is the executable; remaining items are arguments.
+    #[serde(default)]
+    pub command: Option<Vec<String>>,
+}
+
+impl Default for TuiNotificationSoundConfig {
+    fn default() -> Self {
+        Self {
+            enabled: true,
+            command: None,
+        }
+    }
 }

 #[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, Default, JsonSchema)]
diff --git a/codex-rs/core/src/config/config_tests.rs b/codex-rs/core/src/config/config_tests.rs
index b32181c98..e9bbeb871 100644
--- a/codex-rs/core/src/config/config_tests.rs
+++ b/codex-rs/core/src/config/config_tests.rs
@@ -12290,3 +12290,37 @@ fn sqlite_home_env_conflict_reports_an_override() -> std::io::Result<()> {

     Ok(())
 }
+
+#[test]
+fn test_tui_notification_sound_command() {
+    let toml = r#"
+            [tui]
+            notification_sound = { command = ["afplay", "/tmp/codex-done.aiff"] }
+        "#;
+    let parsed = toml::from_str::<ConfigToml>(toml)
+        .expect("deserialize configurable notification sound command");
+    let sound = parsed.tui.expect("tui config").notification_settings.sound;
+
+    assert_eq!(sound.enabled, true);
+    assert_eq!(
+        sound.command,
+        Some(vec![
+            "afplay".to_string(),
+            "/tmp/codex-done.aiff".to_string()
+        ])
+    );
+}
+
+#[test]
+fn test_tui_notification_sound_disabled() {
+    let toml = r#"
+            [tui]
+            notification_sound = { enabled = false }
+        "#;
+    let parsed =
+        toml::from_str::<ConfigToml>(toml).expect("deserialize disabled notification sound");
+    let sound = parsed.tui.expect("tui config").notification_settings.sound;
+
+    assert_eq!(sound.enabled, false);
+    assert_eq!(sound.command, None);
+}
diff --git a/codex-rs/tui/src/app/session_lifecycle.rs b/codex-rs/tui/src/app/session_lifecycle.rs
index 4edbb5abc..712beca4a 100644
--- a/codex-rs/tui/src/app/session_lifecycle.rs
+++ b/codex-rs/tui/src/app/session_lifecycle.rs
@@ -1030,6 +1030,7 @@ impl App {
                 tui.set_notification_settings(
                     self.config.tui_notifications.method,
                     self.config.tui_notifications.condition,
+                    self.config.tui_notifications.sound.clone(),
                 );
                 self.file_search
                     .update_search_dir(self.config.cwd.to_path_buf());
diff --git a/codex-rs/tui/src/app/startup.rs b/codex-rs/tui/src/app/startup.rs
index 97d5bb697..1bb574f1c 100644
--- a/codex-rs/tui/src/app/startup.rs
+++ b/codex-rs/tui/src/app/startup.rs
@@ -105,6 +105,7 @@ impl App {
         tui.set_notification_settings(
             config.tui_notifications.method,
             config.tui_notifications.condition,
+            config.tui_notifications.sound.clone(),
         );

         let harness_overrides =
diff --git a/codex-rs/tui/src/notifications/mod.rs b/codex-rs/tui/src/notifications/mod.rs
index ee6694b35..fdafce985 100644
--- a/codex-rs/tui/src/notifications/mod.rs
+++ b/codex-rs/tui/src/notifications/mod.rs
@@ -1,14 +1,17 @@
 mod bel;
 mod osc9;
+mod sound;

 use std::io;

 use bel::BelBackend;
 use codex_config::types::NotificationMethod;
+use codex_config::types::TuiNotificationSoundConfig;
 use codex_terminal_detection::TerminalInfo;
 use codex_terminal_detection::TerminalName;
 use codex_terminal_detection::terminal_info;
 use osc9::Osc9Backend;
+use sound::play_notification_sound;

 #[derive(Debug)]
 pub enum DesktopNotificationBackend {
@@ -46,10 +49,29 @@ impl DesktopNotificationBackend {
     }
 }

+pub fn notify_with_sound(
+    backend: &mut DesktopNotificationBackend,
+    message: &str,
+    sound_config: &TuiNotificationSoundConfig,
+) -> io::Result<()> {
+    let method = backend.method();
+    backend.notify(message)?;
+
+    if should_play_notification_sound(method) {
+        play_notification_sound(sound_config);
+    }
+
+    Ok(())
+}
+
 pub fn detect_backend(method: NotificationMethod) -> DesktopNotificationBackend {
     DesktopNotificationBackend::for_method(method)
 }

+fn should_play_notification_sound(method: NotificationMethod) -> bool {
+    method != NotificationMethod::Bel
+}
+
 fn supports_osc9(terminal: &TerminalInfo) -> bool {
     matches!(
         terminal.name,
@@ -64,6 +86,7 @@ fn supports_osc9(terminal: &TerminalInfo) -> bool {
 #[cfg(test)]
 mod tests {
     use super::detect_backend;
+    use super::should_play_notification_sound;
     use super::supports_osc9;
     use codex_config::types::NotificationMethod;
     use codex_terminal_detection::TerminalInfo;
@@ -96,6 +119,19 @@ mod tests {
         ));
     }

+    #[test]
+    fn skips_extra_sound_for_bel_notifications() {
+        assert_eq!(
+            should_play_notification_sound(NotificationMethod::Bel),
+            false
+        );
+    }
+
+    #[test]
+    fn plays_extra_sound_for_osc9_notifications() {
+        assert!(should_play_notification_sound(NotificationMethod::Osc9));
+    }
+
     #[test]
     fn supports_osc9_for_supported_terminals() {
         for name in [
diff --git a/codex-rs/tui/src/tui.rs b/codex-rs/tui/src/tui.rs
index 639f88fa9..f4e006718 100644
--- a/codex-rs/tui/src/tui.rs
+++ b/codex-rs/tui/src/tui.rs
@@ -51,6 +51,7 @@ use crate::insert_history::HistoryLineWrapPolicy;
 use crate::insert_history::InsertHistoryMode;
 use crate::notifications::DesktopNotificationBackend;
 use crate::notifications::detect_backend;
+use crate::notifications::notify_with_sound;
 use crate::terminal_hyperlinks::HyperlinkLine;
 use crate::terminal_hyperlinks::plain_hyperlink_lines;
 use crate::tui::event_stream::EventBroker;
@@ -60,6 +61,7 @@ use crate::tui::job_control::SuspendContext;
 use crate::tui::screen_size::ScreenSizePolicy;
 use codex_config::types::NotificationCondition;
 use codex_config::types::NotificationMethod;
+use codex_config::types::TuiNotificationSoundConfig;

 mod event_stream;
 mod frame_rate_limiter;
@@ -594,6 +596,7 @@ pub struct Tui {
     enhanced_keys_supported: bool,
     notification_backend: Option<DesktopNotificationBackend>,
     notification_condition: NotificationCondition,
+    notification_sound: TuiNotificationSoundConfig,
     // Raw terminal-wrapped history needs a non-scroll-region insertion path in Zellij.
     is_zellij: bool,
     // When false, enter_alt_screen() becomes a no-op.
@@ -650,6 +653,7 @@ impl Tui {
             enhanced_keys_supported,
             notification_backend: Some(detect_backend(NotificationMethod::default())),
             notification_condition: NotificationCondition::default(),
+            notification_sound: TuiNotificationSoundConfig::default(),
             is_zellij,
             alt_screen_enabled: true,
             _stderr_guard: stderr_guard,
@@ -665,9 +669,11 @@ impl Tui {
         &mut self,
         method: NotificationMethod,
         condition: NotificationCondition,
+        sound: TuiNotificationSoundConfig,
     ) {
         self.notification_backend = Some(detect_backend(method));
         self.notification_condition = condition;
+        self.notification_sound = sound;
     }

     pub fn frame_requester(&self) -> FrameRequester {
@@ -774,7 +780,7 @@ impl Tui {
         };

         let message = message.as_ref().to_string();
-        match backend.notify(&message) {
+        match notify_with_sound(backend, &message, &self.notification_sound) {
             Ok(()) => true,
             Err(err) => {
                 let method = backend.method();

diff --git a/codex-rs/tui/src/notifications/sound.rs b/codex-rs/tui/src/notifications/sound.rs
new file mode 100644
index 0000000..eb9fe52
--- /dev/null
+++ b/codex-rs/tui/src/notifications/sound.rs
@@ -0,0 +1,197 @@
+use codex_config::types::TuiNotificationSoundConfig;
+use std::path::Path;
+use std::process::Command;
+use std::process::Stdio;
+
+#[derive(Clone, Debug, PartialEq, Eq)]
+struct SoundCommand {
+    program: String,
+    args: Vec<String>,
+}
+
+pub fn play_notification_sound(config: &TuiNotificationSoundConfig) {
+    for command in sound_commands(config, |path| Path::new(path).exists()) {
+        if spawn_sound_command(command) {
+            return;
+        }
+    }
+}
+
+fn spawn_sound_command(command: SoundCommand) -> bool {
+    Command::new(command.program)
+        .args(command.args)
+        .stdin(Stdio::null())
+        .stdout(Stdio::null())
+        .stderr(Stdio::null())
+        .spawn()
+        .is_ok()
+}
+
+fn sound_commands(
+    config: &TuiNotificationSoundConfig,
+    path_exists: impl Fn(&str) -> bool,
+) -> Vec<SoundCommand> {
+    if !config.enabled {
+        return Vec::new();
+    }
+
+    if let Some(command) = &config.command {
+        return configured_sound_command(command).into_iter().collect();
+    }
+
+    sound_commands_for_platform(path_exists)
+}
+
+fn configured_sound_command(command: &[String]) -> Option<SoundCommand> {
+    let (program, args) = command.split_first()?;
+    Some(SoundCommand {
+        program: program.clone(),
+        args: args.to_vec(),
+    })
+}
+
+fn sound_commands_for_platform(path_exists: impl Fn(&str) -> bool) -> Vec<SoundCommand> {
+    let mut commands = Vec::new();
+
+    #[cfg(target_os = "macos")]
+    {
+        const GLASS_SOUND: &str = "/System/Library/Sounds/Glass.aiff";
+
+        if path_exists(GLASS_SOUND) {
+            commands.push(SoundCommand {
+                program: "afplay".to_string(),
+                args: vec![GLASS_SOUND.to_string()],
+            });
+        }
+        commands.push(SoundCommand {
+            program: "osascript".to_string(),
+            args: vec!["-e".to_string(), "beep".to_string()],
+        });
+    }
+
+    #[cfg(target_os = "linux")]
+    {
+        const FREEDESKTOP_SOUND: &str = "/usr/share/sounds/freedesktop/stereo/complete.oga";
+        const ALSA_SOUND: &str = "/usr/share/sounds/alsa/Front_Center.wav";
+
+        commands.push(SoundCommand {
+            program: "canberra-gtk-play".to_string(),
+            args: vec!["-i".to_string(), "bell".to_string()],
+        });
+
+        if path_exists(FREEDESKTOP_SOUND) {
+            commands.push(SoundCommand {
+                program: "paplay".to_string(),
+                args: vec![FREEDESKTOP_SOUND.to_string()],
+            });
+            commands.push(SoundCommand {
+                program: "pw-play".to_string(),
+                args: vec![FREEDESKTOP_SOUND.to_string()],
+            });
+        }
+
+        if path_exists(ALSA_SOUND) {
+            commands.push(SoundCommand {
+                program: "aplay".to_string(),
+                args: vec![ALSA_SOUND.to_string()],
+            });
+        }
+    }
+
+    commands
+}
+
+#[cfg(test)]
+mod tests {
+    use super::SoundCommand;
+    use super::sound_commands;
+    use super::sound_commands_for_platform;
+    use codex_config::types::TuiNotificationSoundConfig;
+
+    fn command(program: &str, args: &[&str]) -> SoundCommand {
+        SoundCommand {
+            program: program.to_string(),
+            args: args.iter().map(|arg| arg.to_string()).collect(),
+        }
+    }
+
+    #[test]
+    fn configured_command_replaces_platform_defaults() {
+        let config = TuiNotificationSoundConfig {
+            enabled: true,
+            command: Some(vec![
+                "afplay".to_string(),
+                "/tmp/codex-done.aiff".to_string(),
+            ]),
+        };
+
+        assert_eq!(
+            sound_commands(&config, |_| true),
+            vec![command("afplay", &["/tmp/codex-done.aiff"])]
+        );
+    }
+
+    #[test]
+    fn disabled_config_has_no_commands() {
+        let config = TuiNotificationSoundConfig {
+            enabled: false,
+            command: None,
+        };
+
+        assert_eq!(sound_commands(&config, |_| true), Vec::new());
+    }
+
+    #[cfg(target_os = "macos")]
+    #[test]
+    fn macos_prefers_system_sound_before_osascript_beep() {
+        assert_eq!(
+            sound_commands_for_platform(|path| path == "/System/Library/Sounds/Glass.aiff"),
+            vec![
+                command("afplay", &["/System/Library/Sounds/Glass.aiff"]),
+                command("osascript", &["-e", "beep"]),
+            ]
+        );
+    }
+
+    #[cfg(target_os = "macos")]
+    #[test]
+    fn macos_falls_back_to_osascript_beep() {
+        assert_eq!(
+            sound_commands_for_platform(|_| false),
+            vec![command("osascript", &["-e", "beep"])]
+        );
+    }
+
+    #[cfg(target_os = "linux")]
+    #[test]
+    fn linux_prefers_canberra_before_file_based_players() {
+        assert_eq!(
+            sound_commands_for_platform(|path| {
+                path == "/usr/share/sounds/freedesktop/stereo/complete.oga"
+            }),
+            vec![
+                command("canberra-gtk-play", &["-i", "bell"]),
+                command(
+                    "paplay",
+                    &["/usr/share/sounds/freedesktop/stereo/complete.oga"]
+                ),
+                command(
+                    "pw-play",
+                    &["/usr/share/sounds/freedesktop/stereo/complete.oga"]
+                ),
+            ]
+        );
+    }
+
+    #[cfg(target_os = "linux")]
+    #[test]
+    fn linux_falls_back_to_alsa_front_center_sound() {
+        assert_eq!(
+            sound_commands_for_platform(|path| path == "/usr/share/sounds/alsa/Front_Center.wav"),
+            vec![
+                command("canberra-gtk-play", &["-i", "bell"]),
+                command("aplay", &["/usr/share/sounds/alsa/Front_Center.wav"]),
+            ]
+        );
+    }
+}
